need install anaconda (if windows)

conda create -n tensorflow_cpu pip python=3.6
activate tensorflow_cpu
pip install --ignore-installed --upgrade tensorflow==1.9

then install these
opencv-python
cvlib
matplotlib